[build]
dockerfilePath = "services/api/dockerfile"
dockerContext = "."
watchPatterns = ["services/api/**"]

[deploy]
healthcheckPath = "/api/public/health/liveness"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
